<Window x:Class="WPF_BudgetApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:WPF_BudgetApp"
        xmlns:userControls="clr-namespace:WPF_BudgetApp.View.UserControls"
        Height="720" Width="1080" WindowStartupLocation="CenterScreen" WindowStyle="None"
        AllowsTransparency="True" Background="Transparent"
        Style="{StaticResource window}">
    
    
    <Border Background="#eff2f7" MouseDown="Border_MouseDown" MouseLeftButtonDown="Border_MouseLeftButtonDown">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="30"/>
                <RowDefinition />
            </Grid.RowDefinitions>
            
            <userControls:WindowBar x:Name="WindowBar" Grid.Row="0" />
            <userControls:ConnectionInterface x:Name="ConnectionInterface" Grid.Row="1" VerticalAlignment="Center" Height="450"/>
            
        </Grid>
    </Border>
</Window>