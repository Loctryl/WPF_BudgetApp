<Window x:Class="WPF_BudgetApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:WPF_BudgetApp"
        xmlns:views="clr-namespace:WPF_BudgetApp.Views"
        xmlns:comps="clr-namespace:WPF_BudgetApp.Components"
        xmlns:viewModels="clr-namespace:WPF_BudgetApp.ViewModel"
        Height="900" Width="1600" WindowStartupLocation="CenterScreen" WindowStyle="None"
        AllowsTransparency="True" Background="Transparent"
        Style="{StaticResource window}">

    <Window.Resources>
        <DataTemplate DataType="{x:Type viewModels:LoginViewModel}">
            <views:LoginView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModels:DashboardViewModel}">
            <views:DashboardView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModels:DebugViewModel}">
            <views:DatabaseView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModels:AccountViewModel}">
            <views:AccountView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModels:DebtViewModel}">
            <views:DebtView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModels:ArchiveViewModel}">
            <views:ArchiveView />
        </DataTemplate>
    </Window.Resources>
    
    <Border Background="{Binding CurrentUser.PrimaryColor}" MouseDown="Border_MouseDown" MouseLeftButtonDown="Border_MouseLeftButtonDown">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="30"/>
                <RowDefinition />
                <RowDefinition Height="25"/>
            </Grid.RowDefinitions>
            
            <comps:WindowBar Grid.Row="0" x:Name="WindowBar" />
            <ContentControl Grid.Row="1" Content="{Binding CurrentVM}" />
            <comps:TaskBar Grid.Row="2" />
        </Grid>
    </Border>
</Window>