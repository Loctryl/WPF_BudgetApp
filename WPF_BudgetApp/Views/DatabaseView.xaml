<UserControl x:Class="WPF_BudgetApp.Views.DatabaseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:WPF_BudgetApp.Views"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="720">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        
        <DataGrid Grid.Row="0"
                  AutoGenerateColumns="False"
                  ItemsSource="{Binding Users}"
        >
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Id}" Header="User Id" />
                <DataGridTextColumn Binding="{Binding SourceName}" Header="Username" />
                <DataGridTextColumn Binding="{Binding Password}" Header="Password" />
                <DataGridTextColumn Binding="{Binding Comment}" Header="Comment" />
                <DataGridTextColumn Binding="{Binding Accounts}" Header="Accounts" />
                <DataGridTextColumn Binding="{Binding CreationDate}" Header="Created On" />
                <DataGridTextColumn Binding="{Binding LastUpdateDate}" Header="Updated On" />
            </DataGrid.Columns>
            
        </DataGrid>
        
        <DataGrid Grid.Row="1"
                  AutoGenerateColumns="False"
                  ItemsSource="{Binding Accounts}"
        >
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Id}" Header="Account Id" />
                <DataGridTextColumn Binding="{Binding SourceName}" Header="Account Name" />
                <DataGridTextColumn Binding="{Binding Symbol}" Header="Symbol" />
                <DataGridTextColumn Binding="{Binding Color}" Header="Color" />
                <DataGridTextColumn Binding="{Binding Balance}" Header="Balance" />
                <DataGridTextColumn Binding="{Binding AppUserId}" Header="UserId" />
                <DataGridTextColumn Binding="{Binding CreationDate}" Header="Created On" />
                <DataGridTextColumn Binding="{Binding LastUpdateDate}" Header="Updated On" />
            </DataGrid.Columns>
            
        </DataGrid>
        
        <DataGrid Grid.Row="2"
                  AutoGenerateColumns="False"
                  ItemsSource="{Binding Categories}"
        >
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Id}" Header="Categories Id" />
                <DataGridTextColumn Binding="{Binding SourceName}" Header="Categories Name" />
                <DataGridTextColumn Binding="{Binding Symbol}" Header="Categories Symbol" />
                <DataGridTextColumn Binding="{Binding Color}" Header="Categories Color" />
                <DataGridTextColumn Binding="{Binding AppUserId}" Header="Categories UserId" />
                <DataGridTextColumn Binding="{Binding CreationDate}" Header="Created On" />
                <DataGridTextColumn Binding="{Binding LastUpdateDate}" Header="Updated On" />
            </DataGrid.Columns>
        </DataGrid>
        
        <DataGrid Grid.Row="3"
                  AutoGenerateColumns="False"
                  ItemsSource="{Binding Debts}"
        >
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Id}" Header="User Id" />
                <DataGridTextColumn Binding="{Binding SourceName}" Header="Username" />
                <DataGridTextColumn Binding="{Binding InitialAmount}" Header="Initial Amount" />
                <DataGridTextColumn Binding="{Binding CurrentDebt}" Header="Current Debt" />
                <DataGridTextColumn Binding="{Binding InterestRate}" Header="Interest Rate" />
                <DataGridTextColumn Binding="{Binding LimitDate}" Header="Limit Date" />
                <DataGridTextColumn Binding="{Binding AppUserId}" Header="App User" />
                <DataGridTextColumn Binding="{Binding CategoryId}" Header="Linked Category" />
                <DataGridTextColumn Binding="{Binding CreationDate}" Header="Created On" />
                <DataGridTextColumn Binding="{Binding LastUpdateDate}" Header="Updated On" />
            </DataGrid.Columns>
            
        </DataGrid>
        
        <DataGrid Grid.Row="4"
                  AutoGenerateColumns="False"
                  ItemsSource="{Binding Transfers}"
        >
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Id}" Header="User Id" />
                <DataGridTextColumn Binding="{Binding SourceName}" Header="Transfer Name" />
                <DataGridTextColumn Binding="{Binding Amount}" Header="Amount" />
                <DataGridTextColumn Binding="{Binding AccountId}" Header="Account" />
                <DataGridTextColumn Binding="{Binding CategoryId}" Header="Category" />
                <DataGridTextColumn Binding="{Binding OperationDate}" Header="Operation Date" />
                <DataGridTextColumn Binding="{Binding DebitDate}" Header="Debit Date" />
                <DataGridTextColumn Binding="{Binding CreationDate}" Header="Created On" />
                <DataGridTextColumn Binding="{Binding LastUpdateDate}" Header="Updated On" />
            </DataGrid.Columns>
            
        </DataGrid>
        
    </Grid>
</UserControl>
