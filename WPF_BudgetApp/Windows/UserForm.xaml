<Window x:Class="WPF_BudgetApp.Windows.UserForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        xmlns:local="clr-namespace:WPF_BudgetApp.Windows"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        mc:Ignorable="d"
        Title="UserForm" Height="450" Width="800" WindowStartupLocation="CenterScreen" WindowStyle="None"
        AllowsTransparency="True" Background="Transparent"
        Style="{StaticResource window}">
    <Border Background="#fff" BorderThickness="2" BorderBrush="Blue" MouseDown="Border_MouseDown">
        <Grid>
        <Grid.ColumnDefinitions>
                <ColumnDefinition Width="20*"/>
                <ColumnDefinition Width="60*"/>
                <ColumnDefinition Width="20*"/>
            </Grid.ColumnDefinitions>
            
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="70*"/>
                    <RowDefinition Height="100"/>
                </Grid.RowDefinitions>
                
                <Grid Grid.Row="0" Name="fields" Visibility="Visible">
                    <Grid.RowDefinitions>
                        <RowDefinition />
                        <RowDefinition />
                    </Grid.RowDefinitions>
                    
                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition />
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition />
                            <ColumnDefinition Width="20"/>
                        </Grid.ColumnDefinitions>
                    
                        <Label Grid.Column="0" Style="{StaticResource labelTextBox}" Content="Username" />
                        <TextBox Grid.Column="0" Name="userNameTB" 
                                 Style="{StaticResource textBox}" 
                                 Height="25" 
                                 Text="{Binding Username}" 
                                 />
                        
                        <Label Grid.Column="2" Style="{StaticResource labelTextBox}" Content="Password" />
                        <PasswordBox Grid.Column="2" Name="userPassword" Style="{StaticResource textBox}" Height="25" PasswordChanged="PasswordBox_OnChange"/>
                        <TextBox Grid.Column="2" Name="passwordTB" 
                                 Style="{StaticResource textBox}" 
                                 Height="25"
                                 Visibility="Collapsed"
                        />
                        
                        <Button Grid.Column="3" MouseLeftButtonDown="EyeButt_Activate" MouseLeftButtonUp="EyeButt_Deactivate" Style="{StaticResource taskBarButtons}" Height="20" Width="20" >
                            <iconPacks:PackIconMaterial Name="EyeButt" Kind="EyeOff" Width="15" Height="15" />
                        </Button>
                    </Grid>
                    
                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition />
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition />
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>
                    
                        <Label Grid.Column="0" Style="{StaticResource labelTextBox}" Content="Primary Color" />
                        <xctk:ColorPicker Grid.Column="0" 
                                          Style="{StaticResource textBox}" 
                                          Height="25" 
                                          ColorMode="ColorCanvas" 
                                          SelectedColor="{Binding PrimaryColor}"
                        />
                        
                        <Label Grid.Column="2" Style="{StaticResource labelTextBox}" Content="Secondary Color" />
                        <xctk:ColorPicker Grid.Column="2" 
                                          Style="{StaticResource textBox}" 
                                          Height="25" 
                                          ColorMode="ColorCanvas" 
                                          SelectedColor="{Binding SecondaryColor}"
                        />
                        
                        <Label Grid.Column="4" Style="{StaticResource labelTextBox}" Content="Tertiary Color" />
                        <xctk:ColorPicker Grid.Column="4" 
                                          Style="{StaticResource textBox}" 
                                          Height="25" 
                                          ColorMode="ColorCanvas" 
                                          SelectedColor="{Binding TertiaryColor}"
                        />
                    
                    </Grid>
                </Grid>
                
                <Grid Grid.Row="1" Height="40" MinWidth="220" HorizontalAlignment="Center">
                    <Button Name="ConfirmButt" Style="{StaticResource windowFormsButton}" Click="ConfirmButt_OnClick" Content="Confirm" HorizontalAlignment="Left" />
                    <Button Style="{StaticResource windowFormsButton}" Click="CancelButt_OnClick" Content="Cancel" HorizontalAlignment="Right"/>
                </Grid>
            </Grid>
        </Grid>
    </Border>
</Window>
