<Window x:Class="WPF_BudgetApp.Windows.ConfirmWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:WPF_BudgetApp.Windows"
        mc:Ignorable="d"
        Title="ConfirmationWindow" Height="225" Width="400" WindowStartupLocation="CenterScreen" WindowStyle="None"
        AllowsTransparency="True" Background="Transparent"
        Style="{StaticResource window}">
    <Border Background="#fff" BorderThickness="2" BorderBrush="Red">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="10*" />
                <ColumnDefinition Width="80*"/>
                <ColumnDefinition Width="10*"/>
            </Grid.ColumnDefinitions>
            
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>
            
            <TextBlock Grid.Column="1" Grid.Row="0" 
                       Height="40" 
                       FontSize="20" 
                       Text="Are you sure ?" 
                       HorizontalAlignment="Center"
                       Style="{StaticResource baseTextBlockStyle}"
                       />
            
            <Grid Grid.Column="1" Grid.Row="1" Height="40" MinWidth="220" HorizontalAlignment="Center">
                <Button Name="ConfirmButt" Click="ConfirmButt_OnClick" Style="{StaticResource windowFormsButton}" Content="Confirm" HorizontalAlignment="Left" />
                <Button Name="CancelButt" Click="CancelButt_OnClick" Style="{StaticResource windowFormsButton}" Content="Cancel" HorizontalAlignment="Right"/>
            </Grid>
        </Grid>
    </Border>
</Window>
