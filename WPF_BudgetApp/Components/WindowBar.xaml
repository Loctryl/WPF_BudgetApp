<UserControl x:Class="WPF_BudgetApp.Views.WindowBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Height="30" d:DesignWidth="300">
    <Border BorderThickness="0 0 0 1" BorderBrush="Black">
        <Grid Background="White">
            
            <Icon:PackIconMaterial Kind="Feather" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10 0" Width="15" Height="15" />
            
            <Button Name="Close_But" Click="Close_But_OnClick" Style="{StaticResource taskBarButtons}" >
                <Icon:PackIconMaterial Kind="Close" Width="15" Height="15" />
            </Button>
            
            <Button Name="Max_But" Click="Max_But_OnClick" Margin="30 0" Style="{StaticResource taskBarButtons}" >
                <Icon:PackIconMaterial Kind="WindowMaximize" Width="10" Height="10" />
            </Button>
            
            <Button Name="Min_But" Click="Min_But_OnClick" Margin="60 0" Style="{StaticResource taskBarButtons}" >
                <Icon:PackIconMaterial Kind="WindowMinimize" Width="10" Height="10" />
            </Button>
        </Grid>
    </Border>
</UserControl>
